{
  "name": "bankco_native_sdk",
  "private": true,
  "version": "1.0.0",
  "description": "Bankco React Native SDK repository with a reusable SDK module and Expo example app.",
  "scripts": {
    "start": "npm --prefix example start",
    "android": "npm --prefix example run android",
    "ios": "npm --prefix example run ios",
    "web": "npm --prefix example run web"
  }
}
